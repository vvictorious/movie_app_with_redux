const initialState = {
    movies: null,
    loading: false
}

const searchReducer = (state = initialState, action) => {
    switch (action.type) {
        default: 
            return state
    }
}

export default searchReducer